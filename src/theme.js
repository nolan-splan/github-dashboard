import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#00ffff',
    },
    secondary: {
      main: '#9327d0',
    },
  },
})

export default theme